{
  "name": "chronomd",
  "version": "0.1.0",
  "private": true,
  "description": "A minimalist personal Markdown publishing platform with timeline-first display",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
